name: ${DEV_PROJECT_NAME}
services:
  wordpress:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        WORDPRESS_SITE_NAME: "My Awesome Blog"
        WORDPRESS_SITE_EMAIL: "admin@example.com"
      target: development
      cache_from:
        - wordpress:latest
      labels:
        maintainer: "Krishan Gordhan"
        version: "1.0"
    ports:
      - "8080:80"
    environment:
      WORDPRESS_DB_HOST: ${WORDPRESS_DB_HOST}
      WORDPRESS_DB_USER: ${WORDPRESS_DB_USER}
      WORDPRESS_DB_PASSWORD: ${WORDPRESS_DB_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_DB_NAME}
    volumes:
      - ./wp-content:/var/www/html/wp-content
    networks:
      wordpress_network:
        ipv4_address: 192.168.1.2
    
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      wordpress_network:
        ipv4_address: 192.168.1.3

volumes:
  db_data:
    labels:
      com.example.description: "Wordpress db storage"
      com.example.owner: "Krishan Gordhan"
      com.example.environment: "Development"

networks:
  wordpress_network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.1.0/24
          gateway: 192.168.1.1